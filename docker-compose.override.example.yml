version: '3'
services:
  auth-db:
    container_name: 'cube-auth-db'
    image: postgres
    ports:
      - 5433:5432
    environment:
      POSTGRES_USER: cube-auth
      POSTGRES_PASSWORD: cube-auth
      POSTGRES_DB: cube-auth
  auth:
    depends_on:
      - 'auth-db'
